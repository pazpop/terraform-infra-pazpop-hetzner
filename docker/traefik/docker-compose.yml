# Logs Docker plafonnés (10 Mo x 3 par conteneur) : le driver par défaut accumule à l'infini
# et peut remplir le disque de la VPS.
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  # Proxy en lecture seule vers le socket Docker (réseau interne traefik-internal
  # uniquement). Monter docker.sock, même en :ro, donnerait un accès root à l'hôte :
  # :ro ne restreint pas les appels à l'API. Seuls CONTAINERS=1 et NETWORKS=1 sont autorisés.
  docker-socket-proxy:
    image: ghcr.io/tecnativa/docker-socket-proxy:v0.5.0
    environment:
      - CONTAINERS=1
      - NETWORKS=1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - traefik-internal
    restart: unless-stopped
    logging: *default-logging
    read_only: true
    tmpfs:
      - /tmp
      - /run
    security_opt:
      - no-new-privileges:true
    mem_limit: 64m
    cpus: 0.1

  # Seul service exposé au monde extérieur sur la VPS (ports 80/443).
  # Les apps (ex: arcadepipe) rejoignent le réseau externe "traefik-public"
  # et se déclarent via labels — aucune n'a besoin de publier de port.
  traefik:
    # >= v3.6.1 requis : les versions antérieures négocient une version
    # d'API Docker figée à 1.24, insuffisante pour Docker >= 29 (minimum
    # relevé à 1.44).
    image: traefik:v3.7
    environment:
      - TRAEFIK_CERTIFICATESRESOLVERS_LETSENCRYPT_ACME_EMAIL=${ACME_EMAIL}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Plus d'accès direct à /var/run/docker.sock : tout passe par
      # docker-socket-proxy ci-dessus.
      - ./traefik.yml:/etc/traefik/traefik.yml:ro
      - ./dynamic:/etc/traefik/dynamic:ro
      - letsencrypt:/letsencrypt
    networks:
      - traefik-public
      - traefik-internal
    restart: unless-stopped
    logging: *default-logging
    security_opt:
      - no-new-privileges:true
    mem_limit: 128m
    cpus: 0.25
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 3s
      start_period: 5s
    depends_on:
      - docker-socket-proxy

networks:
  traefik-public:
    name: traefik-public
    external: true
  traefik-internal:
    name: traefik-internal

volumes:
  letsencrypt:
